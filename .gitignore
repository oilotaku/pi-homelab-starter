# 使用者自己的設定值,含密碼/authkey,絕對不要進版控
.env

# dashboard/html 整個目錄都是產物,不是原始碼:
#   - Vue 前端(dashboard/frontend/)build 出來的靜態檔
#   - cron 腳本(scripts/generate_*.py)定期產生的 JSON
#   - 使用者從 dashboard/config.example.js 複製出來的機器專屬 config.js
# 原始碼在 dashboard/frontend/、dashboard/config.example.js,兩者都進版控。
dashboard/html/

# Pi-hole Docker 執行期資料
pihole/etc-pihole/
pihole/etc-dnsmasq.d/

# Samba 設定備份(腳本執行時自動產生)
samba/*.bak

# 編輯器/系統雜項
*.swp
.DS_Store

# 這台機器實際在跑的下載自動化服務,repo 只公開 dashboard,兩者不進版控
qbittorrent/
rss-manager/
/docker-compose.yml

# cron 腳本 log
scripts/*.log

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Node(前端 build 產物、後端執行期依賴)
node_modules/
dashboard/frontend/dist/
